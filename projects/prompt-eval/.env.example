# cp .env.example .env して値を埋める。.env は git 管理外。

# LLM-as-judge(score --judge)で使う。run / score だけならキー不要
OPENAI_API_KEY=sk-...

# 任意(既定値あり)
# PROMPT_EVAL_TARGET_URL=http://localhost:8100
# PROMPT_EVAL_TIMEOUT=60
