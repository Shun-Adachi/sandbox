"""prompt-eval: プロンプト版を評価データセットで採点するハーネス。

llm-api の POST /v1/extract を評価対象(SUT)として、
「評価ケース × プロンプト版」のマトリクスを回し、レポートを生成する。

処理は 2 段階に分かれている。境界の理由は collect.py / scoring.py の冒頭を参照。

  run   … API を呼んで生の応答を runs/<run_id>/ に保存する(課金が発生する段)
  score … 保存済みの応答を採点してレポートを書く(何度でも無料でやり直せる段)
"""
