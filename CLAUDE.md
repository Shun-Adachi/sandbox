# CLAUDE.md

技術検証用サンドボックスのリポジトリ。テーマごとに `projects/<テーマ名>/` を切って管理する。

## ディレクトリ構成

- `projects/<テーマ名>/src/` … 実装
- `projects/<テーマ名>/docs/` … 設計メモ・スクリーンショット
- 環境(venv, node_modules, Docker 等)は各テーマディレクトリ内で完結させる。
  ルートに共有環境は作らない。

## 運用ルール

- 新テーマは `./.claude/new-sandbox.sh "<テーマ名>"` で雛形から生成し、
  ルート README.md の一覧表に 1 行追加する。
- 各テーマの完成条件は「クローン後、README の記載コマンドだけで動くこと」。
- テーマは原則単体で動かす。他テーマに依存する場合は、依存先・参照の仕組み
  (HTTP か、ファイル参照か等)・影響範囲(どのコマンドが依存するか)をそのテーマの
  README に明示する。コードの import や相対パス参照でテーマをまたがない。
- テーマの README には結果と設計判断だけを書く。試行錯誤の過程は `.claude/tickets/` 側に記録する。
- このリポジトリはあくまで技術を試す場である。特定の案件・企業に関する情報
  (案件名、企業名、契約条件、要件の原文など)は一切書かない・コミットしない。
  そうしたメモは git 管理外の `.claude/docs/` にのみ置く。

## 機密・API キー

- API キーは各テーマの `.env`(git 管理外)に置く。コードや README に直書きしない。
